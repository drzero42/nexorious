// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: platform_storefronts.sql

package db

import (
	"context"
)

const listStorefrontsForPlatform = `-- name: ListStorefrontsForPlatform :many
SELECT s.name, s.display_name, s.icon, s.base_url FROM storefronts s
JOIN platform_storefronts ps ON ps.storefront = s.name
WHERE ps.platform = $1
ORDER BY s.display_name
`

func (q *Queries) ListStorefrontsForPlatform(ctx context.Context, platform string) ([]Storefront, error) {
	rows, err := q.db.Query(ctx, listStorefrontsForPlatform, platform)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	var items []Storefront
	for rows.Next() {
		var i Storefront
		if err := rows.Scan(
			&i.Name,
			&i.DisplayName,
			&i.Icon,
			&i.BaseUrl,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}
