# Repo Migration Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Migrate `drzero42/nexorious-go` into `drzero42/nexorious` as the canonical Go codebase, preserving the Python version on a `python` branch.

**Architecture:** All file changes are made locally in the `nexorious-go` repo first (Phases 2), then force-pushed to `nexorious` main (Phase 3) — the canonical repo is never in a broken state. Phase 1 renames the Python branch first; Phase 4 transfers open issues after the push.

**Tech Stack:** Go 1.25, React/Vite, Helm (bjw-s common library), GitHub Actions, gh CLI

---

## File Map

**Modified in nexorious-go (Phase 2):**
- `go.mod` — module path rename
- All `*.go` files — import path rename
- `go.sum` — regenerated by `go mod tidy`
- `charts/nexorious-go/` → deleted (moved to `deploy/helm/`)
- `deploy/helm/Chart.yaml` — name, home, sources
- `deploy/helm/values.yaml` — comments, image repo, template calls
- `deploy/helm/values.schema.json` — title, descriptions
- `deploy/helm/templates/_helpers.tpl` — all define names
- `deploy/helm/templates/credentials-secret.yaml` — template calls
- `deploy/helm/templates/NOTES.txt` — references
- `deploy/helm/README.md` — title, links
- `CLAUDE.md` — module path in Known Gotchas / Project Structure
- `DEV.md` — Docker image name in examples

**Created in nexorious-go (Phase 2):**
- `deploy/docker/docker-compose.yml`
- `.github/workflows/test.yaml`
- `.github/workflows/build-push.yaml`
- `README.md` (rewrite)
- `renovate.json`
- `.env.example`
- `docs/igdb-setup.md`

---

## Task 1: Phase 1 — Rename nexorious main → python

**Files:** GitHub API only (no local files)

- [ ] **Step 1: Rename the branch**

```bash
gh api -X POST /repos/drzero42/nexorious/branches/main/rename -f new_name=python
```

Expected output: JSON with `"name": "python"`. GitHub automatically makes `python` the default branch.

- [ ] **Step 2: Verify python is now the default branch**

```bash
gh repo view drzero42/nexorious --json defaultBranchRef --jq '.defaultBranchRef.name'
```

Expected output: `python`

---

## Task 2: Update Go module path

**Files:**
- Modify: `go.mod`
- Modify: all `*.go` files
- Modify: `go.sum` (via `go mod tidy`)

- [ ] **Step 1: Update go.mod**

```bash
sed -i 's|module github.com/drzero42/nexorious-go|module github.com/drzero42/nexorious|' go.mod
```

- [ ] **Step 2: Update all import paths in Go source files**

```bash
find . -name "*.go" -not -path "./.git/*" \
  -exec sed -i 's|github.com/drzero42/nexorious-go|github.com/drzero42/nexorious|g' {} +
```

- [ ] **Step 3: Regenerate go.sum**

```bash
go mod tidy
```

Expected: exits 0, `go.sum` updated.

- [ ] **Step 4: Verify the build compiles**

```bash
make build
```

Expected: `nexorious` binary produced, no errors.

- [ ] **Step 5: Commit**

```bash
git add -u
git commit -m "chore: rename Go module path to github.com/drzero42/nexorious"
```

---

## Task 3: Relocate and rename Helm chart

**Files:**
- Delete: `charts/nexorious-go/` (entire directory, moved)
- Create: `deploy/helm/` (moved from `charts/nexorious-go/`)
- Modify: `deploy/helm/Chart.yaml`
- Modify: `deploy/helm/values.yaml`
- Modify: `deploy/helm/values.schema.json`
- Modify: `deploy/helm/templates/_helpers.tpl`
- Modify: `deploy/helm/templates/credentials-secret.yaml`
- Modify: `deploy/helm/templates/NOTES.txt`
- Modify: `deploy/helm/README.md`

- [ ] **Step 1: Move the chart directory**

```bash
mkdir -p deploy
mv charts/nexorious-go deploy/helm
rmdir charts
```

- [ ] **Step 2: Replace nexorious-go in all Helm files**

```bash
find deploy/helm -type f \
  -exec sed -i 's|nexorious-go|nexorious|g' {} +
```

This handles all occurrences in `Chart.yaml` (name, home, sources), `values.yaml` (comments, image repo, template calls), `values.schema.json` (title, descriptions), `templates/_helpers.tpl` (all define names), `templates/credentials-secret.yaml` (include calls), `templates/NOTES.txt` (chart references), and `README.md`.

- [ ] **Step 3: Rebuild Helm dependencies**

```bash
helm repo add bjw-s https://bjw-s-labs.github.io/helm-charts/
helm dependency update deploy/helm/
```

Expected: `Chart.lock` updated, `charts/` directory populated under `deploy/helm/`.

- [ ] **Step 4: Verify Helm lint passes**

```bash
helm lint --strict deploy/helm/ \
  --set nexorious.secretKey=x \
  --set nexorious.postgresql.password=x \
  --set nexorious.igdbClientId=x \
  --set nexorious.igdbClientSecret=x
```

Expected: `1 chart(s) linted, 0 chart(s) failed`

- [ ] **Step 5: Commit**

```bash
git add deploy/helm/
git rm -r charts/
git commit -m "chore: move Helm chart from charts/nexorious-go to deploy/helm and rename to nexorious"
```

---

## Task 4: Create deploy/docker/docker-compose.yml

**Files:**
- Create: `deploy/docker/docker-compose.yml`

- [ ] **Step 1: Create the file**

```bash
mkdir -p deploy/docker
```

Create `deploy/docker/docker-compose.yml` with this content:

```yaml
# Simple deployment using the published container image.
#
# Services:
#   db    — PostgreSQL 18 database
#   app   — Nexorious binary (embeds frontend, runs migrations on startup)
#
# Usage:
#   docker compose up -d        # Start all services
#   docker compose down         # Stop (keeps volumes)
#   docker compose down -v      # Stop and remove volumes (fresh DB)
#
# Configuration: copy .env.example to .env at the repo root and fill in values.

services:
  db:
    image: docker.io/postgres:18-alpine
    environment:
      POSTGRES_USER: nexorious
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:?POSTGRES_PASSWORD is required}
      POSTGRES_DB: nexorious
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nexorious"]
      interval: 5s
      timeout: 5s
      retries: 5

  app:
    image: ghcr.io/drzero42/nexorious:dev
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy
    environment:
      DATABASE_URL: postgres://nexorious:${POSTGRES_PASSWORD}@db:5432/nexorious?sslmode=disable
      SECRET_KEY: ${SECRET_KEY:?SECRET_KEY is required}
      IGDB_CLIENT_ID: ${IGDB_CLIENT_ID:?IGDB_CLIENT_ID is required}
      IGDB_CLIENT_SECRET: ${IGDB_CLIENT_SECRET:?IGDB_CLIENT_SECRET is required}

volumes:
  postgres_data:
```

- [ ] **Step 2: Commit**

```bash
git add deploy/docker/docker-compose.yml
git commit -m "chore: add deploy/docker/docker-compose.yml for simple container deployment"
```

---

## Task 5: Create .github/workflows/test.yaml

**Files:**
- Create: `.github/workflows/test.yaml`

- [ ] **Step 1: Create the workflow file**

```bash
mkdir -p .github/workflows
```

Create `.github/workflows/test.yaml`:

```yaml
---
name: Test

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  workflow_call:

jobs:
  go-tests:
    name: Go Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Run Go tests
        env:
          # Tests use testcontainers to spin up their own PostgreSQL container.
          # Point testcontainers at the Docker daemon available on GitHub-hosted runners.
          DOCKER_HOST: unix:///var/run/docker.sock
        run: go test -timeout 600s ./...

  go-lint:
    name: Go Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: go.mod

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v6
        with:
          version: latest

  frontend-tests:
    name: Frontend Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: "npm"
          cache-dependency-path: ui/frontend/package-lock.json

      - name: Install dependencies
        run: cd ui/frontend && npm ci

      - name: Run frontend tests
        run: cd ui/frontend && npm run test

  type-check:
    name: Type Check
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: "npm"
          cache-dependency-path: ui/frontend/package-lock.json

      - name: Install dependencies
        run: cd ui/frontend && npm ci

      - name: Build (generates routeTree.gen.ts) and type check
        # `npm run build` runs `vite build && tsc --noEmit`. The vite build step triggers
        # the TanStack Router plugin to generate src/routeTree.gen.ts (gitignored),
        # which tsc requires. Running `npm run check` alone would fail without it.
        run: cd ui/frontend && npm run build

  helm-lint:
    name: Helm Lint
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Helm
        uses: azure/setup-helm@v4

      - name: Add bjw-s Helm repo
        run: helm repo add bjw-s https://bjw-s-labs.github.io/helm-charts/

      - name: Build Helm dependencies
        run: helm dependency build deploy/helm/

      - name: Lint Helm chart
        run: |
          helm lint --strict deploy/helm/ \
            --set nexorious.secretKey=x \
            --set nexorious.postgresql.password=x \
            --set nexorious.igdbClientId=x \
            --set nexorious.igdbClientSecret=x
```

- [ ] **Step 2: Commit**

```bash
git add .github/workflows/test.yaml
git commit -m "ci: add Go test workflow"
```

---

## Task 6: Create .github/workflows/build-push.yaml

**Files:**
- Create: `.github/workflows/build-push.yaml`

- [ ] **Step 1: Create the workflow file**

Create `.github/workflows/build-push.yaml`:

```yaml
---
name: Build and Push Container Image

on:
  push:
    branches: [main]
  release:
    types: [published]

jobs:
  test:
    name: Run Tests
    uses: ./.github/workflows/test.yaml

  build-push:
    name: Build and Push
    runs-on: ubuntu-latest
    needs: test

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Log in to ghcr.io
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Extract image metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/drzero42/nexorious
          tags: |
            # Release tags: v1.2.3 → 1.2.3
            type=semver,pattern={{version}}
            # Main branch builds: YYYYMMDD-<short-sha> + dev
            type=raw,value={{date 'YYYYMMDD'}}-{{sha}},enable=${{ github.ref == 'refs/heads/main' }}
            type=raw,value=dev,enable=${{ github.ref == 'refs/heads/main' }}

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          build-args: |
            VERSION=${{ github.ref_type == 'tag' && github.ref_name || 'dev' }}
            COMMIT=${{ github.sha }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  build-push-chart:
    name: Build and Push Helm Chart
    runs-on: ubuntu-latest
    needs: build-push

    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Compute chart version
        id: chart-version
        run: |
          if [[ "${{ github.event_name }}" == "release" ]]; then
            VERSION="${{ github.event.release.tag_name }}"
            VERSION="${VERSION#v}"
            APP_VERSION="$VERSION"
          else
            SHORT_SHA=$(git rev-parse --short HEAD)
            DATE=$(date +%Y%m%d)
            VERSION="0.0.0-dev-${DATE}-${SHORT_SHA}"
            APP_VERSION="dev"
          fi
          echo "version=$VERSION" >> $GITHUB_OUTPUT
          echo "app_version=$APP_VERSION" >> $GITHUB_OUTPUT

      - name: Add bjw-s Helm repo
        run: helm repo add bjw-s https://bjw-s-labs.github.io/helm-charts/

      - name: Build Helm dependencies
        run: helm dependency update deploy/helm/

      - name: Package Helm chart
        id: helm-package
        run: |
          PKG=$(helm package deploy/helm/ \
            --version "${{ steps.chart-version.outputs.version }}" \
            --app-version "${{ steps.chart-version.outputs.app_version }}" \
            | awk '{print $NF}')
          echo "path=$PKG" >> "$GITHUB_OUTPUT"

      - name: Log in to ghcr.io (Helm)
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | \
            helm registry login ghcr.io \
              --username "${{ github.actor }}" \
              --password-stdin

      - name: Push Helm chart
        run: helm push "${{ steps.helm-package.outputs.path }}" oci://ghcr.io/drzero42/charts
```

- [ ] **Step 2: Commit**

```bash
git add .github/workflows/build-push.yaml
git commit -m "ci: add build-and-push workflow for container image and Helm chart"
```

---

## Task 7: Rewrite README.md

**Files:**
- Modify: `README.md`

- [ ] **Step 1: Replace README.md with Go-specific content**

Replace the entire contents of `README.md` with:

```markdown
# Nexorious

> [!WARNING]
> **Work in Progress — Not Ready for Use**
> Nexorious is under active development and is not ready for production use or general adoption. Expect breaking changes, missing features, incomplete documentation, and rough edges. Use at your own risk.

A self-hostable web application for managing personal video game collections with comprehensive IGDB integration for tracking, organizing, and discovering games across multiple platforms and storefronts.

Nexorious was inspired by [Darkadia](https://darkadia.com) (RIP), a beloved game collection tracker that is no longer operating.

## Features

- **IGDB-Only Game Database**: All games sourced from the Internet Game Database (IGDB) with comprehensive metadata, cover art, ratings, and completion time estimates
- **Multi-Platform Game Tracking**: Support for Steam, Epic Games Store, PlayStation, Xbox, Nintendo, GOG, and physical media
- **Sync Integrations**: Automatic library sync from Steam, Epic Games Store (via legendary-gl), and PlayStation Network (PSN)
- **Rich Game Discovery**: Search and import games from IGDB's extensive database with automatic metadata population
- **Progress Tracking**: Track play status, personal ratings, time played, and detailed notes
- **Bulk Operations**: Import from CSV exports (Darkadia format) with intelligent conflict resolution
- **Single Binary**: Go backend with embedded React SPA — one process serves everything
- **Modern Tech Stack**: Go backend with React + Vite frontend

## Quick Start

### Prerequisites

- **Go 1.25+**
- **Node.js 24+** with npm
- **PostgreSQL 16+**
- **Nix + devenv** (optional, recommended for reproducible development environment)
- **IGDB API Credentials** (required) — see [IGDB Setup Guide](docs/igdb-setup.md)

### Development Setup

#### Option 1: Using devenv (Recommended)

```bash
git clone https://github.com/drzero42/nexorious.git
cd nexorious
devenv shell       # enters reproducible shell with Go, Node, golangci-lint, make
devenv up -d       # starts PostgreSQL in the background
make               # builds frontend then Go binary
export DATABASE_URL="postgres:///nexorious"  # devenv uses a Unix socket
./nexorious        # starts server on :8000; visits /migrate if schema is pending
```

#### Option 2: Manual Setup

```bash
git clone https://github.com/drzero42/nexorious.git
cd nexorious
make               # builds frontend (npm ci + vite build) then Go binary
export DATABASE_URL="postgres://user:password@localhost:5432/nexorious"
./nexorious        # starts server; auto-runs migrations on first launch
```

### Initial Setup

1. **First Run**: Navigate to `http://localhost:8000` — you will be prompted to create an admin account
2. **Migrations**: Run automatically on startup; the app gate-keeps all routes until the schema is ready
3. **IGDB Credentials**: Configure via the admin settings page after first login

## Production Deployment

### Docker Compose

The simplest production-like deployment uses the published container image:

```bash
cp .env.example .env   # fill in SECRET_KEY, IGDB_CLIENT_ID, IGDB_CLIENT_SECRET, POSTGRES_PASSWORD
docker compose -f deploy/docker/docker-compose.yml up -d
```

The app container automatically runs migrations on startup before serving traffic.

### Kubernetes / Helm

A Helm chart is published to `oci://ghcr.io/drzero42/charts` and is built on the [bjw-s common library](https://bjw-s-labs.github.io/helm-charts/).

```bash
helm install nexorious oci://ghcr.io/drzero42/charts/nexorious \
  --set nexorious.secretKey="$(openssl rand -hex 32)" \
  --set nexorious.igdbClientId=YOUR_CLIENT_ID \
  --set nexorious.igdbClientSecret=YOUR_CLIENT_SECRET \
  --set nexorious.postgresql.password="$(openssl rand -hex 16)"
```

See `deploy/helm/values.yaml` for the full values reference.

### Environment Variables

```bash
# Required
DATABASE_URL=postgres://user:password@host:5432/nexorious?sslmode=disable
SECRET_KEY=your-secret-key-here           # generate: openssl rand -hex 32
IGDB_CLIENT_ID=your-igdb-client-id
IGDB_CLIENT_SECRET=your-igdb-client-secret

# Optional
PORT=8000                                  # default: 8000
STORAGE_PATH=/path/to/storage             # default: ./storage
BACKUP_PATH=/path/to/backups              # default: ./storage/backups
LOG_LEVEL=info                             # default: info
```

### Production Checklist

- [ ] PostgreSQL configured
- [ ] `DATABASE_URL` set
- [ ] `SECRET_KEY` set to a cryptographically random value
- [ ] IGDB API credentials configured
- [ ] Storage directory writable
- [ ] Backup procedures in place

## Development

### Key Commands

| Task | Command |
|------|---------|
| Build everything | `make` |
| Build backend only | `make build` |
| Build frontend only | `make frontend` |
| Run server | `./nexorious` or `./nexorious serve` |
| Run migrations only | `./nexorious migrate` |
| Migration status | `./nexorious migrate status` |
| Run Go tests | `go test -timeout 600s ./...` |
| Run frontend tests | `npm run test` (from `ui/frontend/`) |
| Type check frontend | `npm run check` (from `ui/frontend/`) |
| Lint Go | `golangci-lint run` |
| API client | `slumber` |

### Project Structure

```
nexorious/
├── cmd/nexorious/        # Entry point — wires config, DB, Echo, workers
├── internal/
│   ├── api/             # Echo route handlers (games, auth, sync, import, export, …)
│   ├── db/              # Bun ORM models and SQL migrations
│   ├── worker/          # River job workers (sync, import, export, metadata)
│   ├── scheduler/       # Periodic maintenance jobs (cleanup, backup polling)
│   ├── services/        # IGDB client, Steam/PSN sync, game matching
│   ├── auth/            # JWT generation and validation
│   └── config/          # Environment variable config
├── ui/
│   ├── frontend/        # React + Vite SPA source
│   └── migrate/         # Standalone migration UI (embedded in binary)
├── deploy/
│   ├── helm/            # Helm chart (bjw-s common library)
│   └── docker/          # Docker Compose for simple deployments
└── docs/                # Documentation
```

### Tech Stack

- **Backend**: Go 1.25, Echo v5, Bun ORM, River job queue, pgx/v5
- **Frontend**: React 19, Vite 6, TypeScript, TanStack Router + Query, Tailwind CSS v4, shadcn/ui
- **Database**: PostgreSQL 16+
- **Testing**: stdlib `testing` + testcontainers-go (Go); Vitest + @testing-library/react (frontend)

### Database Migrations

Migrations live in `internal/db/migrations/` as timestamped SQL files (`YYYYMMDDHHmmss_name.up.sql` / `.down.sql`). They are discovered and run automatically on startup. To add a new migration:

```bash
# Create migration files (replace timestamp and name)
touch internal/db/migrations/20260101000001_my_change.up.sql
touch internal/db/migrations/20260101000001_my_change.down.sql
```

Check migration status without applying:

```bash
./nexorious migrate status
```

## Testing

### Go

```bash
go test -timeout 600s ./...

# Single package with verbose output
go test ./internal/api/... -run TestGamesList -v

# With coverage
go test -timeout 600s -cover ./...
```

Tests use testcontainers-go to spin up a real PostgreSQL container — no mocking the database. Requires Docker or Podman socket access.

### Frontend

```bash
cd ui/frontend

npm run test           # run all tests
npm run check          # TypeScript type check
```

## Sync Integrations

### Steam

Configure via the admin settings page: enter your Steam Web API key and Steam ID. See the API documentation for full details.

### PlayStation Network (PSN)

Configure via the admin settings page with your PSN NPSSO token.

### Epic Games Store

Requires `legendary-gl` installed and authenticated on the host. See [issue #45](https://github.com/drzero42/nexorious/issues/45) for container image support status.

## API Documentation

The API is self-documenting. With the server running, explore endpoints via `slumber` (included in the devenv shell):

```bash
slumber
```

See `DEV.md` for the full development guide including database reset procedures and the two-terminal Vite dev server workflow.

## AI-Assisted Development

Nexorious was built with extensive use of AI tooling, specifically [Claude Code](https://claude.ai/code) by Anthropic. AI assistance was used throughout the project for code generation, architecture decisions, debugging, and documentation.

This is an intentional choice — Nexorious is partly an experiment in what AI-assisted software development can produce. If you have strong objections to AI-generated or AI-assisted code, Nexorious may not be the right project for you.

## Trademarks and Copyright

All mentioned trademarks, brand names, and logos for gaming platforms and storefronts (including but not limited to PlayStation, Xbox, Nintendo, Steam, Epic Games Store, GOG, Apple App Store, Google Play Store, and others) are the property of their respective owners. These trademarks are used solely for identification and compatibility purposes.

The use of these trademarks and brand names does not imply any affiliation, endorsement, or partnership with the respective companies. All rights to these trademarks remain with their original owners.

The logos and icons used in this application are sourced from SVG Repo and other public repositories under various open-source licenses (MIT, CC0, Logo License, etc.).

## License

MIT License — see LICENSE file for details.

---

**Self-hosted game collection management made simple.**
```

- [ ] **Step 2: Commit**

```bash
git add README.md
git commit -m "docs: rewrite README for Go version"
```

---

## Task 8: Add renovate.json and .env.example

**Files:**
- Create: `renovate.json`
- Create: `.env.example`

- [ ] **Step 1: Create renovate.json**

Create `renovate.json`:

```json
{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended"
  ]
}
```

- [ ] **Step 2: Create .env.example**

Create `.env.example`:

```bash
# Nexorious environment variables
# Copy this file to .env and fill in the values.
# The .env file is gitignored and never committed.

# Database connection
DATABASE_URL=postgres://nexorious:yourpassword@localhost:5432/nexorious?sslmode=disable

# Security — generate a random value: openssl rand -hex 32
SECRET_KEY=your-secret-key-here

# IGDB API credentials — required for game search and metadata
# Setup guide: docs/igdb-setup.md  |  Twitch console: https://dev.twitch.tv/console
IGDB_CLIENT_ID=your-igdb-client-id
IGDB_CLIENT_SECRET=your-igdb-client-secret

# Docker Compose only — PostgreSQL password
# (DATABASE_URL is set automatically from this when using deploy/docker/docker-compose.yml)
POSTGRES_PASSWORD=yourpassword
```

- [ ] **Step 3: Commit**

```bash
git add renovate.json .env.example
git commit -m "chore: add renovate.json and .env.example"
```

---

## Task 9: Port docs/igdb-setup.md

**Files:**
- Create: `docs/igdb-setup.md`

- [ ] **Step 1: Copy from Python repo and update**

```bash
cp /home/abo/workspace/home/nexorious/docs/igdb-setup.md docs/igdb-setup.md
```

Then edit `docs/igdb-setup.md` — replace the two Python-specific configuration examples under "Step 5: Configure Nexorious":

Replace **Option A** (currently references `backend/` directory):
```markdown
#### Option A: Using a `.env` file (recommended for development)

Create or edit `.env` at the repository root:

```bash
IGDB_CLIENT_ID=your_client_id_here
IGDB_CLIENT_SECRET=your_client_secret_here
```
```

Replace **Option B** (currently references Python Docker Compose services):
```markdown
#### Option B: Using Docker Compose

Add the environment variables to your `deploy/docker/docker-compose.yml` or pass them via a `.env` file at the repository root (the compose file reads `IGDB_CLIENT_ID` and `IGDB_CLIENT_SECRET` from there automatically).
```

Replace **Option C** (currently references restarting the Python backend):
```markdown
#### Option C: Using system environment variables

```bash
export IGDB_CLIENT_ID=your_client_id_here
export IGDB_CLIENT_SECRET=your_client_secret_here
```
```

Replace **Step 6: Restart the Backend** with:
```markdown
### 6. Restart the Application

After configuring the credentials, restart the Nexorious server:

```bash
# If using Docker Compose
docker compose -f deploy/docker/docker-compose.yml restart app

# If running directly
# Stop the current process and restart
./nexorious serve
```
```

Also remove the entire "API Endpoints Reference" section at the bottom (it references Python-specific endpoints that don't apply).

- [ ] **Step 2: Commit**

```bash
git add docs/igdb-setup.md
git commit -m "docs: port igdb-setup.md from Python repo, update for Go"
```

---

## Task 10: Update CLAUDE.md and DEV.md

**Files:**
- Modify: `CLAUDE.md`
- Modify: `DEV.md`

- [ ] **Step 1: Update CLAUDE.md module path references**

```bash
sed -i 's|github.com/drzero42/nexorious-go|github.com/drzero42/nexorious|g' CLAUDE.md
```

Verify the change looks correct:

```bash
grep -n "nexorious" CLAUDE.md | grep -i "github\|module" | head -10
```

- [ ] **Step 2: Update DEV.md Docker image examples**

```bash
sed -i 's|nexorious-go:local|nexorious:local|g' DEV.md
```

Verify:

```bash
grep -n "nexorious" DEV.md | grep ":local"
```

Expected: all occurrences now say `nexorious:local`.

- [ ] **Step 3: Commit**

```bash
git add CLAUDE.md DEV.md
git commit -m "chore: update nexorious-go references in CLAUDE.md and DEV.md"
```

---

## Task 11: Final verification before push

**Files:** none modified

- [ ] **Step 1: Run Go tests**

```bash
go test -timeout 600s ./...
```

Expected: all tests pass.

- [ ] **Step 2: Run frontend type check**

```bash
cd ui/frontend && npm run build && cd ../..
```

Expected: exits 0.

- [ ] **Step 3: Helm lint**

```bash
helm lint --strict deploy/helm/ \
  --set nexorious.secretKey=x \
  --set nexorious.postgresql.password=x \
  --set nexorious.igdbClientId=x \
  --set nexorious.igdbClientSecret=x
```

Expected: `1 chart(s) linted, 0 chart(s) failed`

- [ ] **Step 4: Verify no remaining nexorious-go import paths in Go source**

```bash
grep -r "github.com/drzero42/nexorious-go" --include="*.go" .
```

Expected: no output.

---

## Task 12: Phase 3 — Push to nexorious and restore default branch

**Files:** none (remote operations only)

- [ ] **Step 1: Add nexorious as a remote**

```bash
git remote add nexorious git@github.com:drzero42/nexorious.git
```

- [ ] **Step 2: Fetch to verify the remote is accessible**

```bash
git fetch nexorious
```

Expected: fetches `python` branch. No `main` branch exists yet.

- [ ] **Step 3: Force-push current branch as nexorious main**

```bash
git push nexorious HEAD:main --force
```

Expected: `* [new branch] HEAD -> main`

- [ ] **Step 4: Set main as the default branch**

```bash
gh repo edit drzero42/nexorious --default-branch main
```

- [ ] **Step 5: Verify**

```bash
gh repo view drzero42/nexorious --json defaultBranchRef --jq '.defaultBranchRef.name'
```

Expected: `main`

---

## Task 13: Phase 4 — Transfer open issues

Transfer 8 open issues from `nexorious-go` to `nexorious`. For each issue: ensure labels exist on the target repo, create the issue, then close the original with a pointer.

**Files:** none (GitHub API only)

- [ ] **Step 1: Ensure the `enhancement` label exists on nexorious**

```bash
gh label create enhancement \
  --repo drzero42/nexorious \
  --description "New feature or request" \
  --color "a2eeef" 2>/dev/null || echo "label already exists"
```

- [ ] **Step 2: Transfer issues in a loop**

Run this script which reads each issue's full body and creates it on nexorious, then closes the original:

```bash
for ISSUE_NUM in 47 46 45 36 34 33 31 25; do
  # Read the source issue
  TITLE=$(gh issue view "$ISSUE_NUM" --repo drzero42/nexorious-go --json title --jq '.title')
  BODY=$(gh issue view "$ISSUE_NUM" --repo drzero42/nexorious-go --json body --jq '.body')
  LABELS=$(gh issue view "$ISSUE_NUM" --repo drzero42/nexorious-go --json labels --jq '[.labels[].name] | join(",")')

  # Create on nexorious
  if [ -n "$LABELS" ]; then
    NEW_URL=$(gh issue create \
      --repo drzero42/nexorious \
      --title "$TITLE" \
      --body "$BODY" \
      --label "$LABELS")
  else
    NEW_URL=$(gh issue create \
      --repo drzero42/nexorious \
      --title "$TITLE" \
      --body "$BODY")
  fi

  NEW_NUM=$(echo "$NEW_URL" | grep -o '[0-9]*$')
  echo "Created nexorious#$NEW_NUM from nexorious-go#$ISSUE_NUM"

  # Close the original with a pointer
  gh issue close "$ISSUE_NUM" \
    --repo drzero42/nexorious-go \
    --comment "Moved to drzero42/nexorious#${NEW_NUM}"
done
```

- [ ] **Step 3: Verify all 8 issues are open on nexorious**

```bash
gh issue list --repo drzero42/nexorious --state open
```

Expected: 8 issues (plus the Renovate Dependency Dashboard).

- [ ] **Step 4: Verify all 8 original issues are closed on nexorious-go**

```bash
gh issue list --repo drzero42/nexorious-go --state open
```

Expected: no open issues.
