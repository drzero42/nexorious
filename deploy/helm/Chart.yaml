apiVersion: v2
name: nexorious
description: Self-hosted game collection
type: application
version: 0.1.0
# appVersion drives the default image tag for the nexorious image. "latest"
# is dev-friendly only; real deployments should override
# controllers.nexorious.containers.main.image.tag (and the matching
# initContainers.migrate.image.tag) to pin a specific release.
appVersion: "latest"
kubeVersion: ">=1.28.0-0"
home: https://github.com/drzero42/nexorious
sources:
  - https://github.com/drzero42/nexorious
maintainers:
  - name: Nexorious
keywords:
  - games
  - collection
  - self-hosted
dependencies:
  - name: common
    repository: https://bjw-s-labs.github.io/helm-charts/
    version: 5.0.1
